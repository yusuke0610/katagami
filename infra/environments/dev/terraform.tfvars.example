# cp terraform.tfvars.example terraform.tfvars して値を埋める。
# terraform.tfvars は gitignore 対象（プロジェクト ID 等の実値を置く）。
environment  = "dev"
project_id   = "your-project-dev"
cors_origins = "http://localhost:5173"
# deployer_service_account_email = "deployer@your-project-dev.iam.gserviceaccount.com"
