# ローカル開発用の環境変数テンプレート。cp .env.example .env して使う。
# env 名の正本は backend/app/core/env_keys.py（drift は make lint-env-keys が検証する）。

# アプリケーションのバージョン識別子（/health が返す。CI/デプロイ時に注入する想定）
APP_VERSION=dev

# CORS 許可オリジン（カンマ区切り）
CORS_ORIGINS=http://localhost:5173
